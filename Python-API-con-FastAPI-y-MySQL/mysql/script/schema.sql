CREATE DATABASE easyTemplate;
GRANT ALL PRIVILEGES ON easyTemplate.* TO 'ruben'@'%'; 
FLUSH PRIVILEGES;

-- USE easyTemplate;

-- CREATE TABLE users (
--   user_id INT NOT NULL,
--   user_name VARCHAR(30) NOT NULL,
--   user_surname VARCHAR(30) NOT NULL,
--   user_nick VARCHAR(30) NOT NULL,
--   PRIMARY KEY (user_id),
--   UNIQUE (user_nick)
-- );


